Parameters:
  CodeKey:
    Type: String
    Description: >-
      The S3 object key containing the Lambda zip
  CodeVersion:
    Type: String
    Description: >-
      The S3 object version containing the Lambda zip
  ResourceCodeKey:
    Type: String
    Description: >-
      The s3 object key containing the lambda resource zip
  ResourceCodeVersion:
    Type: String
    Description: >-
      The s3 object version containing the lambda resource zip